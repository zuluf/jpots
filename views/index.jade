extends layout

block content
    header
        h1 jQuery Popup Overlay Test Suite
        h2 Maximum z-index values fix

    #content
        button(data-action="popup-append") Append popup
        button(data-action="popup-prepend") Prepend popup
        button(data-action="max-zindex") Add a lunatic z-index element to DOM

        div(data-handler="popup")
        div(data-handler="stats")

